# OFFLINE-MIRROR-PLAN.md — 工作区离线镜像（macOS + 安卓模式1）

> **一句话**：把外置盘上的工作区**整份复制**到本机内部存储，硬盘不在手上时照样读、照样写笔迹；
> 硬盘接回来时，用**三方合并**把两边的改动并到一起。
>
> 状态：**方案，未实现**。定方案日 2026-08-30。

---

## 1. 先把 framing 立对：镜像不是"多端同步"，也不违反单写者红线

现有红线是「同一时间只能有一端打开同一个工作区」。镜像**不触碰这条**——因为：

> **镜像是一个独立的工作区**（自己的路径、自己的 `WorkspaceManager`/`LibraryStore` 实例），
> 只是额外带了两样东西：① 一条「我从谁那来」的血缘；② 一个**离线的、用户显式触发的**合并工具。

任何时刻仍然只有一端在写某一个库文件。没有租约、没有心跳、没有实时通道、没有后台自动写。
「同步」是一个**用户按下去才会发生的批处理动作**，跟 git 的 `merge` 同性质，不是 Dropbox 那种常驻守护。

这个 framing 决定了后面所有取舍：**不引入分布式一致性，只引入一次性的三方 diff。**

---

## 2. 分档交付

| 档 | 能力 | 工程量 | 评价 |
|---|---|---|---|
| **L0 只读镜像** | 复制一份到本地，打开即禁写 | 小（两端各 1~2 天） | **单独交付价值很低**——UniReader 的价值在笔迹，只能读的镜像约等于一个 PDF 阅读器 |
| **L1 可写镜像 + 回灌** | 镜像上正常写笔迹/加书；接回硬盘后把改动并回去 | **主体工程** | ✅ **这才是用户真正要的那档** |
| **L2 双向合并** | 硬盘那边的新改动也拉进镜像 | L1 的**对称扩展**，增量很小 | ✅ 顺手做掉，否则用户马上会问「那反过来呢」 |
| **L3 实时/自动同步** | 插上就自动合并、多端并发写 | 大 | ❌ **明确不做**（见 §11） |

**已定（2026-08-30 用户拍板）：一次做到 L2（双向）。** L0 不单独发版，它只是 L1 的一个开关（「这本没带 PDF」「镜像角标」这些 UI 在 L1 里全要复用）。

---

## 3. 核心机制：base 指纹 + 三方合并

### 3.1 为什么必须要三方

只有两方（镜像 vs 源盘）比不出**删除**：某条笔迹「源盘有、镜像没有」，到底是
"我在镜像上擦掉了它" 还是 "你在源盘上新写的"？——两方比对无解，硬删除又没留墓碑。

引入第三方 **base = 建镜像那一刻的样子**，一切就确定了：

| base | mine（镜像） | theirs（源盘） | 判定 |
|---|---|---|---|
| 无 | 有 | 无 | 镜像新增 → 写进源盘 |
| 无 | 无 | 有 | 源盘新增 → 拉进镜像 |
| 有 | 无 | 有（未变） | 镜像删除 → 源盘也删 |
| 有 | 有（未变） | 无 | 源盘删除 → 镜像也删 |
| 有 | 变了 | 未变 | 镜像修改 → 写进源盘 |
| 有 | 未变 | 变了 | 源盘修改 → 拉进镜像 |
| 有 | 变了 | 变了（且不同） | **冲突** → §6 |
| 有 | 无 | 变了 | **删除 vs 修改冲突** → 保留修改（不丢数据） |

### 3.2 base 存什么：**指纹表，不是整库副本**

镜像库里新建一张表（**只在镜像里存在，源库永远不认识它**）：

```sql
CREATE TABLE sync_base (
  tbl     TEXT NOT NULL,      -- 'note' / 'document' / 'ink_layer' / 'scratch_pad' / 'variant'
  row_id  TEXT NOT NULL,      -- 主键
  fp      TEXT NOT NULL,      -- 该行内容指纹（见 3.3）
  PRIMARY KEY (tbl, row_id)
) WITHOUT ROWID;
```

三种备选方案的对比（**这是本方案最关键的一次取舍**）：

| 方案 | 写路径改动 | 运行时开销 | 空间 | 抗错性 | 结论 |
|---|---|---|---|---|---|
| **oplog（触发器记每次写）** | 零（用 SQL 触发器） | 每次写多一条 INSERT | 中 | **差**：任何一次绕过触发器的写就永久错位，且触发器 DDL 要跨端逐字一致 | ❌ |
| **base = 整库副本** | 零 | 零 | **库翻倍**（note 是大表） | 好 | ⭕ 备选 |
| **base = 指纹表** | 零 | 零 | **~1/40**（5 万笔 ≈ 2.5MB） | 好 | ✅ **选它** |

指纹表还有一个 oplog 给不了的性质：**它不依赖"从建镜像起每一次写都被记上"这个连续性假设**——
任何时候都能重新算 diff，中途崩溃/断电/被别的版本的 app 写过，都不影响正确性。

### 3.3 指纹算法（**跨端契约，Mac ↔ 安卓必须字节一致**）

按表的**列声明顺序**把每列编码后拼接，列间以 `0x1F` 分隔，整串 SHA-256，取**前 16 个 hex 字符**：

| 列类型 | 编码 |
|---|---|
| NULL | 单字节 `0x00` |
| INTEGER | 十进制 ASCII（含负号） |
| REAL | **IEEE754 双精度 8 字节小端原始字节**（**不走十进制文本**——`%.17g` 的两端实现不保证一致，这是必踩的坑） |
| TEXT | UTF-8 原文 |
| BLOB | 原字节 |

配套：`spike/mirror-fp-test.swift` + 安卓 `MirrorFpTest`，**共用一组跨端向量**（同 `wire-cross-test` 的既有纪律）。

---

## 4. 哪些表同步、哪些是设备本地事实

**这张表是整个设计的另一半核心。搞错一格就会出现「同步完把书全变成路径失效」这种事故。**

| 表 / 列 | 同步 | 理由 |
|---|---|---|
| `note`（kind 0 文字 / 1 AI / 2 笔迹 / 3 高亮 / 4 草稿纸笔迹） | ✅ **必须** | 用户全部产出都在这张表 |
| `ink_layer` | ✅ | 笔迹图层注册表，不带过去笔迹就没了归属 |
| `scratch_pad` | ✅ | 草稿纸 |
| `document`：`title` / `group_name` / `canvas_mode` / `sort_order` | ✅ | 用户显式改的属性 |
| `document`：`read_page` / `read_frac` / `read_zoom` / `read_hfrac` | ✅ **LWW** | "接着上次读"是用户预期；镜像场景下两端不会并行读（硬盘不在手上），冲突罕见 |
| `document`：`last_opened_at` | ✅ 取较大值 | **刻意不进指纹**（§3.3）：进了的话「在镜像上翻开过」就会把整行标记成「改过」，干跑预览里满屏都是无意义的条目。合并时无条件取 max 即可 |
| `variant` | ✅ | 加书才会新增；`content_hash` 唯一，天然去重 |
| **`location`** | ❌ **绝不同步** | **「文件在哪」是设备本地事实**。镜像把外部文件内化成 `PDFs/xxx.pdf`，这条路径在源盘上毫无意义；反过来源盘的绝对路径在平板上必然失效。同步它 = 制造大批假的"路径失效" |
| `ocr_page` | ✅ 双向 `INSERT OR IGNORE` | 按 `content_hash` 缓存、纯 additive、两端算出的内容等价 → **不需要 base**，直接互补即可 |
| `meta.note_types` | ✅ LWW | 工作区级配置 |
| `meta.workspace_name` | ✅ LWW | 小事 |
| `meta.open_documents` | ❌ | 「本机开着哪几篇」，设备本地 |
| `meta.schema_version` / `created_at` | ❌ | 库自身属性 |
| `meta.workspace_id` / `mirror_*` / `offline_checkouts` | ❌ | 血缘元数据，见 §5 |
| `sync_base` | ❌ | 镜像私有 |

---

## 5. 身份与血缘

### 5.1 工作区需要一个稳定 id（**当前没有，要补**）

现在 `meta` 只有 `workspace_name`（会被改名）。新增：

- `meta.workspace_id` = UUID，**缺失时首次需要用到就地补写**（不改 schema 版本，只是 meta 多一行；
  安卓 `SELECT *` 忽略未知 key，零兼容成本）。
- 用途：镜像认得出「我的源是谁」，插上任意一块盘时能自动匹配，而不是靠路径（路径必变）或名字（会重名）。

### 5.2 源库记「借出了几份」

```
meta.offline_checkouts = [
  { mirror_id, device_id, device_name, taken_at, note_count_at_checkout }, ...
]
```

- 建镜像时追加一条（**这是对源库的一次写**——所以建镜像要求源盘可写，本来就是）。
- 同步成功后**不删**这条，只更新 `last_synced_at`（镜像是长期存在的，不是一次性 checkout）。
- 用户在镜像那端「放弃镜像」或在源盘端「清除记录」才移除。
- **它不是锁**：Mac/安卓打开一个有 checkout 记录的工作区时，只在书库顶栏显示一行
  「有 2 份离线镜像（小米Pad 6 · 8月30日）」，**不阻塞任何操作**。

### 5.3 镜像库记「我从谁那来」

```
meta.mirror_of        = <源 workspace_id>
meta.mirror_id        = <本镜像 UUID>
meta.mirror_created_at
meta.mirror_source_hint = "卷标 + 上次见到的路径"   -- 只为提示用户"把那块盘插上"，不作为判据
meta.mirror_last_synced_at
```

判据永远是 `workspace_id`，`source_hint` 只用来给一句人话提示。

---

## 6. 冲突规则

同一行两端相对 base 都改了：

| 情形 | 规则 |
|---|---|
| 有 `updated_at` 的表（`note` / `scratch_pad`） | **LWW，`updated_at` 新的赢** |
| 无 `updated_at` 的表（`document` / `ink_layer` / `variant`） | **保留源盘（theirs）**，并报告。可冲突字段只有 title/group_name/read_* 这类低价值项，保守选一边即可 |
| **一端删、一端改** | **保留"改"**（不丢用户数据优先），报告 |
| 一端删、一端未动 | 照删 |

**红线：合并绝不静默删除笔迹。** 每一次删除都必须能追溯到"对方 base 里有、现在没有"这个显式证据；
拿不准的一律保留 + 进报告。

**冲突报告**必须是人话，不是行 id：
> 《高等数学》第 87 页的一条笔迹：两端都改过，保留了平板上较新的那份（8月30日 14:22）

---

## 7. PDF 文件：库全量、文件选择性

**关键简化：`library.sqlite` 始终整份镜像，PDF 才按需拷。**

平板内部存储装不下一块几十 GB 的硬盘，但库本身只有几十 MB。于是：

- 镜像里**所有书都看得见、所有笔记都在**，只是部分书没带 PDF；
- 打开没带 PDF 的书 → 一句「这本没有离线，插回硬盘再看」+「现在拷过来」（源盘在场时可用）；
- 建镜像时按书勾选，或按分组（`document.group_name`）整组勾。

**外部文件的内化**：源工作区里 `in_workspace=0` 的书（`is_relative=1` 的同卷文件、或 Mac 绝对路径），
镜像时**能解析到就拷进镜像的 `PDFs/`**，并在**镜像库**里加一条 `in_workspace=1` 的 location。
因为 §4 定了 location 不参与同步，这条本地事实不会污染源库。

### 🔴 镜像上加书**必须拷进工作区**（2026-08-30 用户拍板）

在镜像里加书时，PDF **一律拷进镜像的 `PDFs/`**（`in_workspace=1`），**不允许外部引用**。
理由：外部引用的路径在源盘那端毫无意义，回灌时要么丢文件、要么得凭空发明一套"把平板上某个
外部路径的文件搬过去"的语义。强制内化之后，回灌的文件搬运退化成**一条规则**：

> 镜像里 `in_workspace=1` 的 location，其 `content_hash` 在源盘没有对应文件 → 拷进源盘 `PDFs/` + 建 location。

**这一步不走 diff**，是一遍幂等的"补齐"，可重入、拷一半再来一次也对。

两端落地：
- **安卓模式1 已经天然满足**——`local/PdfImport.kt` 本来就是「探页数 → SHA-256 → 拷进 `PDFs/` → 入库」，
  无需改动。
- **Mac 要加约束**：当前工作区是镜像（`meta.mirror_of` 非空）时，导入走「拷进工作区」这一条路，
  不给"仅引用外部文件"的选项。

---

## 8. 流程

### 8.1 建镜像（源盘在场）

1. 选内容（哪些书带 PDF）+ 选目标位置；**先 `StatFs`/`volumeAvailableCapacity` 算够不够**。
2. 源库 `PRAGMA wal_checkpoint(TRUNCATE)`。
3. **`VACUUM INTO '<镜像>/UniReader/library.sqlite'`** —— 一句 SQL 拿到一致快照，顺带压缩，
   不需要停写、不需要 `sqlite3_backup_*`、不怕 `-wal` 落下（Mac 的 libsqlite3 与安卓 API 30+ 都自带 SQLite 3.27+）。
   **比 `cp` 三个文件（`.sqlite`/`-wal`/`-shm`）可靠得多**——安卓端已经踩过"只拷 .sqlite 丢 WAL"的坑。
4. 拷勾选的 PDF 到镜像 `PDFs/`（临时名 + 原子 rename，中途拔盘不留半个文件）。
5. 在镜像库算一遍 `sync_base` 指纹（§3.3）。
6. 写镜像 `meta.mirror_*`；写源库 `meta.offline_checkouts`。

### 8.2 用镜像

和普通工作区完全一样。唯一差别在 UI：书库顶栏一枚**离线镜像角标** + 「源：XXX（未连接）」+
「同步」按钮（源盘不在场时置灰）。没带 PDF 的书在列表里灰一档。

### 8.3 同步（源盘接回来）

1. **先备份源库**：`VACUUM INTO <源>/UniReader/backup/library-<ts>.sqlite`，保留最近 3 份。廉价保险，必做。
2. `ATTACH` 三库（源=main / 镜像 / 指纹表在镜像里），按 §4 白名单逐表跑 §3.1 的判定表。
3. **干跑预览**（必做，不给"直接执行"的入口）：
   > 写入硬盘：新增笔迹 132、删除 8、修改 3、新增文档 1（《XXX》，需拷 42MB）
   > 拉回本机：新增笔迹 0、新增文档 2（PDF 共 1.2GB，可稍后再拷）
   > 冲突 1 条 → 展开看
4. 用户确认 → **一个事务**内应用双向行操作；文件搬运在事务外、幂等、可重入。
5. 重算 `sync_base`（此刻两端在白名单表上已一致）；更新 `meta.mirror_last_synced_at` 与源库 checkout 条目。

### 8.4 多镜像天然可用

平板一份、笔记本一份 —— 每份镜像有自己的 base，**互不知道对方存在**。
A 同步完源盘变了，B 再同步时会把 A 的改动当作 "theirs 侧新增/删除" 正确拉进来。
删除也能正确二次传播（A 删 → 源盘删 → B 的 base 有、theirs 没有 → B 也删）。**UUID 不重用，不存在 ABA 问题。**

---

## 9. 两端落地要点

### macOS

- 入口：侧栏工作区菜单加「制作离线镜像…」；镜像窗口的工具栏/侧栏加「同步到源盘…」。
- 镜像与源是**两个不同路径 = 两个 `WorkspaceManager` 实例**，天然可同时开着。
  同步时若源工作区**正在本进程开着**，必须走 `WorkspaceRegistry` 拿到那个实例的 store 来写，
  **不能另开一个连接**（违反「同一路径同一实例」红线）。
- 镜像默认位置：`NSSavePanel` 让用户选，默认 `~/Documents/`。名字与源同名（靠角标区分，不靠改名）。
- 复用 `WorkspaceManager.createWorkspace` 的"不覆盖已有工作区"纪律。

### 安卓模式1

- 镜像位置（**2026-08-30 用户拍板**）：**共享存储 `/storage/emulated/0/Documents/UniReader/`**，不是 app 私有目录。
  理由：镜像是用户的数据，**卸载 app 不该丢笔迹**；且现有 `StorageScan`/`FileBrowser` 直接就能看见它，零新代码。
- 入口：启动页/书库的工作区芯片下拉加「做成离线副本…」；镜像的书库顶栏加「同步」。
- 建镜像/同步全部经 `StoreQueue` 的独占线程（**慢卷上是秒级，主线程做必 ANR**——§9.5 的既有教训），
  界面走进度条 + 可取消。
- 用 `Db` 现有的降级逻辑：FAT32/exFAT 源盘上 WAL 建不起来时 `VACUUM INTO` 仍然可用（它不依赖 WAL）。
- 空间检查必须做在拷贝之前，且要**留出余量**（内部存储写满会连累整个系统）。

---

## 10. 风险与对策

| 风险 | 对策 |
|---|---|
| **同步是对用户最宝贵数据的批量写** | ① 强制干跑预览；② 合并前自动备份源库；③ 单事务、失败整体回滚；④ 删除必须有显式证据 |
| 中途拔盘 | SQLite 事务保证库不坏；文件拷贝用临时名 + 原子 rename；补齐逻辑幂等可重入 |
| 源盘同时被另一端打开（跨设备，无解） | checkout 记录 + 打开时的信息提示；**不做锁**（红线：不引入分布式一致性） |
| 指纹算法两端不一致 → 全表误判为"改过" | 跨端向量测试（同 `wire-cross-test`）；REAL 走 IEEE754 原始字节而非十进制文本 |
| 镜像与源同名，用户搞混 | UI 角标 + 「源：XXX」一行说明；扫描列表里镜像单独一段 |
| 平板空间不够 | 库全量 + PDF 选择性（§7）；拷前算空间并留余量 |
| 用户把镜像又镜像一次 | **禁止**：`meta.mirror_of` 存在时不给"制作镜像"入口 |

---

## 11. 明确不做（被砍清单）

- ❌ **实时/自动同步**：插上盘自动合并、后台守护。自动写用户数据太危险，只做"检测到源盘 → 提示可同步"。
- ❌ **锁/租约**：checkout 是**信息**不是**锁**。做锁就要处理租约过期、强制解锁、时钟漂移，收益远小于成本。
- ❌ **字段级/笔迹级合并**：冲突粒度就是一行。不做"这一笔的前半段用你的、后半段用我的"。
- ❌ **PDF 内容冲突**：`content_hash` 是文档身份，同 hash 就是同一份文件，不存在内容冲突。
- ❌ **同步 `location`**：见 §4，同步它就是制造事故。
- ❌ **给 schema 加 rev 列 / 墓碑表**：会污染跨端 schema 契约、要改所有写路径。指纹表在库外解决同样的问题。
- ❌ **web 采集页参与**：镜像是"本地有一整个工作区"的能力，采集页没有本地库。

---

## 12. 分期与验收

| 期 | 内容 | 验收 |
|---|---|---|
| **M1** | `meta.workspace_id` 补写 + 指纹算法两端实现 + 跨端向量测试 | `spike/mirror-fp-test.swift` 与安卓 `MirrorFpTest` 同组向量全绿 |
| **M2** | 建镜像（`VACUUM INTO` + PDF 选择性 + 内化外部文件 + base 快照） | Mac 建的镜像在安卓能打开、笔迹位置与源盘逐页一致；反之亦然 |
| **M3** | 镜像 UI（角标、无 PDF 的书、源盘在场检测） | 真机：镜像上正常写笔迹、擦除、加书 |
| **M4** | 三方 diff + 干跑预览 + 冲突报告（**只算不写**） | 造 8 种情形（§3.1 判定表）各一条，预览结果逐条正确 |
| **M5** | 应用合并（单事务 + 备份 + 文件补齐） | 合并后两端白名单表逐行一致；中途拔盘不坏库 |
| **M6** | 双向（L2）+ 多镜像 | A/B 两份镜像轮流同步，删除能二次传播 |

---

## 13. 已拍板（2026-08-30）

| 问题 | 决定 |
|---|---|
| 阅读进度是否跨镜像同步 | ✅ **同步，LWW**（`updated_at` 无 → 用 §6 的无时间戳规则）；`last_opened_at` 取 max 且不进指纹 |
| 安卓镜像位置 | ✅ `/storage/emulated/0/Documents/UniReader/`（用户可见、卸载不丢、现有 `StorageScan` 直接扫得到） |
| 档位 | ✅ **一次做到 L2（双向）** |
| 镜像上加的书 | ✅ **必须拷进工作区**（`in_workspace=1`），不允许外部引用 —— 见 §7 |
